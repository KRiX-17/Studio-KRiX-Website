# Hooks

Keep focused, reusable client-side hooks here when a feature genuinely requires
browser state. The current site uses Framer Motion’s reduced-motion hook inside
the single `Reveal` client component, so no project-specific hook is needed yet.
